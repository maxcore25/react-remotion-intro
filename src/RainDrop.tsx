import React from 'react';

const RainDrop: React.FC = () => {
	return (
		<div
			style={{
				width: 100,
				height: 100,
				background: 'lightblue',
				borderRadius: '50%',
			}}
		/>
	);
};

export default RainDrop;
